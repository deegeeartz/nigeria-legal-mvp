# Domain-specific database repository modules.
# Each module contains the persistence logic for a single domain.
# Import from these modules for new code; app/db.py remains as a
# backward-compatible re-export shim.
